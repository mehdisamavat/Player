pluginManagement {
    repositories {
        maven {
            url "https://maven.google.com"
        }
        gradlePluginPortal()
        google()
        jcenter()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven {
            url "https://maven.google.com"
        }
        google()
        jcenter()
        mavenCentral()
    }
}
rootProject.name = "ExoMIne"
include ':app'
